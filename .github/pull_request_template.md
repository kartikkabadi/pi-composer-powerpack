## Summary

<!-- What changed and why? -->

## Checklist

- [ ] README / docs updated if user-facing behavior changed
- [ ] `package.json` `pi` field and `files` list are accurate
- [ ] `pnpm run check:secrets` passes
- [ ] `pnpm run pack:dry` passes
- [ ] No secrets, session files, auth material, or machine-specific paths in the diff

## Test plan

<!-- How did you verify? e.g. pi install smoke, slash commands -->
